# 🟡 대기중
# SPEC_{SystemName} — {시스템 한글명}

## 1. 개요

| 항목 | 내용 |
|---|---|
| **Component Name** | `{컴포넌트명}` |
| **기능 요약** | {한 줄 설명} |
| **기획서 참조** | `기획서/1.핵심 시스템/{파일명}.md` |
| **모듈화 레이어** | `Core / Combat / Meta / UI / Bootstrap` |

---

## 2. Execution Space

| 처리 단계 | 실행 공간 | 설명 |
|---|---|---|
| {단계명} | `[server only]` / `[client only]` | {설명} |

---

## 3. Properties

| Property Name | Type | Sync | Default | 설명 |
|---|---|---|---|---|
| `{변수명}` | `{타입}` | `@Sync` / `None` | `{기본값}` | {설명} |

---

## 4. 사용 서비스 & API

| 서비스/API | 용도 |
|---|---|
| `{서비스명}` | {용도} |

---

## 5. 로직 흐름

### 5-1. {시나리오명}
```
{흐름 기술}
```

---

## 6. Maker 배치 (Codex MCP 실행) ⚠️ 필수 검토

> **이 섹션은 생략 불가.** 해당 없으면 "해당 없음"으로 명시할 것.
> Codex는 MCP를 통해 `.ui`/`.map`/`.model` JSON을 직접 수정합니다.

### 6-1. UI 엔티티 (`DefaultGroup.ui`)

| 작업 | 엔티티 | 컴포넌트 | 초기 상태 | 위치/크기 | 비고 |
|---|---|---|---|---|---|
| `추가`/`수정`/`제거` | `{엔티티명}` | `{컴포넌트 목록}` | `enable: true/false` | `{Position/Size}` | {설명} |

### 6-2. 맵 엔티티 (`{맵이름}.map`)

| 작업 | 엔티티 경로 | 컴포넌트 | 속성 | 비고 |
|---|---|---|---|---|
| `추가`/`수정`/`제거` | `/maps/{맵}/{엔티티}` | `{컴포넌트 목록}` | `{속성값}` | {설명} |

### 6-3. 글로벌/모델 (`DefaultPlayer.model` 등)

| 작업 | 대상 파일 | 변경 내용 | 비고 |
|---|---|---|---|
| `수정` | `{파일명}` | `{변경 사항}` | {설명} |

### 6-4. 컴포넌트 부착 관계

> 어떤 엔티티에 어떤 스크립트 컴포넌트를 부착해야 하는지 명시

| 엔티티 경로 | 부착할 컴포넌트 | 설정할 Property | 비고 |
|---|---|---|---|
| `/maps/{맵}/{엔티티}` | `script.{컴포넌트명}` | `{Property}={값}` | {설명} |

> **작성 가이드:**
> - UI 엔티티: position, size, anchor, pivot 등 레이아웃 정보 포함
> - 맵 엔티티: TransformComponent Position 포함
> - 컴포넌트 부착: 어떤 엔티티에 어떤 스크립트를 붙이는지 반드시 명시
> - Property 초기값: Maker에서 설정해야 할 값 명시

---

## 7. 연동 컴포넌트

| 컴포넌트 | 레이어 | 연동 방식 |
|---|---|---|
| `{컴포넌트명}` | `{레이어}` | {설명} |

---

## 8. 주의/최적화 포인트

- {항목}

---

## 9. Codex 구현 체크리스트

- [ ] `@Component` 어트리뷰트, `{레이어}` 레이어
- [ ] `_GRUtil` 사용 (중복 유틸 금지)
- [ ] `[server only]` / `[client only]` 분리
- [ ] `nil`/`isvalid` 방어 + `pcall` 보호
- [ ] **Maker 배치 (§6) 완료** — UI/맵/모델/컴포넌트 부착 전부 반영
- [ ] `기획서/4.부록/Code_Documentation.md` 업데이트
- [ ] 완료 후 상태 `🟢 완료`로 변경

---

## 메타 정보

| 항목 | 내용 |
|---|---|
| **작성자** | Antigravity (TD) |
| **담당자** | Codex |
| **작성일** | {날짜} |
| **상태** | 🟡 대기중 |
